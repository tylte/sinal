import { Box } from "@chakra-ui/react";
import React from "react";
import { Player } from "../utils/types";

interface InGameLobbyProps {
  player: Player;
}

export const InGameLobby: React.FC<InGameLobbyProps> = ({ player }) => {
  return <Box></Box>;
};
