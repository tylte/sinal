describe("Sum testing", () => {
  test("adds 1 + 2 to equal 3", () => {
    expect(1 + 2).not.toBe(10);
  });
  test("adds 1 + 2 to equal 3", () => {
    expect(1 + 2).not.toBe(11);
  });
  test("adds 1 + 2 to equal 3", () => {
    expect(1 + 2).toBe(3);
  });
  test("adds 1 + 2 to equal 3", () => {
    expect(1 + 2).toBe(3);
  });
});
